deploys:
  initial:
    ch:
      config:
        - "SECRET_TOKEN"
        - "UID"
        - "MAIN_APP_UID"
        - "G5_CLIENT_UID"
        - "HEROKU_APP_NAME"
        - "HEROKU_API_KEY"
        - "HEROKU_USERNAME"
        - "ID_RSA"
        - "G5_CONFIGURATOR_FEED_URL"
      tasks:
        - "rake db:migrate"
        - "rake deploy:tasks"
      addons:
        - "redistogo:nano"
        - "rediscloud:20"
        - "papertrail:choklad"
        - "memcachier:dev"
    chd:
      config:
        - "SECRET_TOKEN"
        - "UID"
        - "MAIN_APP_UID"
        - "G5_CLIENT_UID"
        - "HEROKU_APP_NAME"
        - "HEROKU_API_KEY"
        - "HEROKU_USERNAME"
        - "ID_RSA"
        - "G5_CONFIGURATOR_FEED_URL"
        - "APP_DISPLAY_NAME"
      tasks:
        - "rake db:migrate"
        - "rake deploy:tasks"
      addons:
        - "redistogo:nano"
        - "rediscloud:20"
        - "papertrail:choklad"
    cpns:
      config:
        - "SECRET_KEY_BASE"
      tasks:
        - "rake db:migrate"
        - "rake deploy:tasks"
      addons:
        - "papertrail:choklad"
